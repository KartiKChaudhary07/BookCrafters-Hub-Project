import * as pack from "../package.json";

const G_API_URL = "http://localhost:5000";
const G_APP_VERSION_CODE = pack?.version;
const G_APP_PLATFORM_BY = "IntegraMinds";

export { G_API_URL, G_APP_VERSION_CODE, G_APP_PLATFORM_BY };
